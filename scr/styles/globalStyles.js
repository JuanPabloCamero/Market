import { Button, StyleSheet, Text } from "react-native"

const styles = StyleSheet.create({
    container:{
        flex: 1,
        backgroundColor: 'grey'
    },
    text:{
        fontSize: 18,
        fontWeight: 'bold',
        fontFamily: 'system'
    },
    button:{
        backgroundColor: '#ffe33',
        fontSize: 25
    }
});

export default styles;